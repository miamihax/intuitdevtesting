fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
anthropic>=0.40
python-dotenv>=1.0
httpx>=0.27
python-multipart>=0.0.9
pytesseract>=0.3.10
pdf2image>=1.17
pypdf>=5.0
watchdog>=4.0
Pillow>=10.0
vercel_blob>=0.4.2
# vercel_blob's own dependencies -- Vercel's build doesn't resolve these
# transitively from vercel_blob's metadata, so they need to be listed here
# directly or the deployed function fails to import with ModuleNotFoundError.
requests>=2.31
tqdm>=4.66
